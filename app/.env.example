# Trivium frontend — environment configuration
#
# All variables are OPTIONAL. The defaults expect the API to run at
# localhost:3001 with the Vite dev proxy.

# Override the API base URL. Leave empty in dev to use the proxy defined
# in vite.config.js (which forwards /api → http://localhost:3001).
VITE_API_URL=
